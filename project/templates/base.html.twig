<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <div>header</div>
    {% block content %}<div>header</div>{% endblock %}
    <h1>{% trans %}Symfony is great!{% endtrans %}</h1>

    <a href="{{ path('connect') }}"><button>dashboard</button></a>
    {% for locale in locales %}
        <a href="{{ path('changeLocale', {'locale': locale,'url': app.request.get('_route')}) }}"><button>{{ locale }}</button></a>
    {% endfor %}

    <div>footer</div>

{% block javascripts %}{% endblock %}
</body>
</html>
